---
"gen-referentiel-core": minor
---

Ajoute l'API `/api/versions` pour lister les versions disponibles dans les fiches. Refactorise le script de génération pour itérer sur les locales configurées au lieu de scanner le filesystem, et inclut les versions distinctes dans le JSON généré.
